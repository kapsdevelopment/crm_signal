# Local development
DATABASE_URL=postgresql://crm_signal:crm_signal_dev_password@localhost:54329/crm_signal

# Docker Compose Postgres defaults
POSTGRES_DB=crm_signal
POSTGRES_USER=crm_signal
POSTGRES_PASSWORD=crm_signal_dev_password
POSTGRES_PORT=54329

# Local raw snapshot/storage path. Ignored by git via /data/.
LOCAL_STORAGE_PATH=./data/storage
